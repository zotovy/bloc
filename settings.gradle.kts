pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "BLoC"
include(":shared")
enableFeaturePreview("VERSION_CATALOGS")
